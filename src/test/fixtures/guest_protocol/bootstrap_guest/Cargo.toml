[package]
name = "bootstrap_guest"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
extism-pdk = { version = "=1.4.1", default-features = false }
serde_json = "1"
